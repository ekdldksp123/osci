import { xcss } from "@atlaskit/primitives"

export const gridStyles = xcss({
  width: "100%"
})

export const closeContainerStyles = xcss({
  gridArea: "close"
})

export const titleContainerStyles = xcss({
  gridArea: "title"
})
